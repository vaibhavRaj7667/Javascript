// = assignment operator
// == comparison operator(compare if value are equal)
// === strict equality operator (compare if values & data type are equal)
// != inequality oprator
// !== strict inequality operatot

const pi = 3.14;

if (pi == "3.14") {
  // if i use strict equal operator. this shows else message
  console.log("yes its equal");
} else {
  console.log("no its not");
}
